# Example environment variables for Docker 

# server secret keys 
API_KEY=your_server_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Note: Client-side keys are not needed here. 
# The client Docker build will use its own .env from client/.env at runtime. 
# Passing client keys through docker-compose can lead to them being undefined in React.